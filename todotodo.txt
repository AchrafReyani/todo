#checkpoints (unordered)

#create, read, update and delete todo's from list. (crud operations)

#make a database that the todo list uses to add, edit and delete todo's

#make project live so others can use it

#make sure the project is secure
    use auth0 for authorization (if i login i should have access to more things for example, basically add roles)
    use google to login so everyone has their own todo list

#use model view controller design pattern (mvc)

#clean looking ui both mobile and desktop
    take bootstrap and download a template to use
    dont try to make the html and css yourself

#language options and save language options for users